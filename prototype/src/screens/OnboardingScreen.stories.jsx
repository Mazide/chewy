import { OnboardingScreen } from './OnboardingScreen.jsx';

export default {
  title: 'Screens/OnboardingScreen',
  component: OnboardingScreen,
};

/** Name him → loop card → honesty card → hero asks about pushes himself. */
export const FirstLaunch = { args: { defaultName: 'Chewy' } };
