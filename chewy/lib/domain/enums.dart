enum Units { metric, imperial }

enum Goal { lose, maintain, gain }

enum MealTag { breakfast, lunch, dinner, snack, none }

enum PortionSize { s, m, l }

enum PlateStatus { processing, ready, failed }

enum AnalysisSource { ai, manual }

enum DrinkType { water, unsweetened, sweetOther }

